---
story_id: 0.0.0
epic: epic-0.0-platform-estate-provisioned
repo: ctam-architecture
bus_version: arch-v1.1
frs: []
nfrs: []
depends_on_stories: []
sprint_status_key: 0-0-0-author-the-github-manual-setup-runbook
---

# Story 0.0.0: Author the GitHub manual-setup runbook (`ctam-architecture/runbooks/github-setup.md`)

Status: review

## Story

As a **platform engineer**,
I want a single manual GitHub web-UI setup runbook covering repo creation, branch protection, team access and the per-repo agent-enforcement wiring,
So that **every one of the 16 repos is created identically and safely without the `gh` CLI, and no scaffold story has to reverse-engineer its own preconditions**.

## Acceptance Criteria

1. **Given** the `gh` CLI is unavailable to engineers and denied to agent sessions (**D10**; enforced by `_arch/agent-rules/enforcement/claude/hooks/block-git-writes.sh`, which denies all `gh`/`hub` subcommands),
   **And** the runbook is written at `ctam-architecture/runbooks/github-setup.md`,
   **When** an engineer follows it to create a new CTAM repository,
   **Then** it documents creation **via the GitHub web UI** under the HMCTS org — **private**, named per the CNP convention (`ctam-<service>`; `{product}-shared-infrastructure` for the shared estate),
   **And** it states explicitly that the HMCTS template's `setup-new-repo.sh` **must not** be used, because it makes the repository **PUBLIC**,
   **And** it documents branch protection on `main` via *Settings → Branches*: require PR review, require status checks, require linear history,
   **And** it documents `CODEOWNERS` and team access scoped to the owning team (per AR29),
   **And** no step anywhere in the runbook invokes `gh`.

2. **Given** the repository exists and is empty,
   **When** the engineer moves on to scaffolding,
   **Then** the runbook states the boundary explicitly — `ctam-scaffold.sh` performs **local** scaffolding and pushes to the **pre-created** remote over plain `git`; it never creates or configures a repository,
   **And** the runbook names itself the precondition of Stories 0.0.1, 0.1.1, 0.2.1, 0.2.4 and 0.5.1, so the dependency is documented in one place rather than restated in five.

3. **Given** a newly created repository must be able to host a cold agent session,
   **When** the engineer completes the per-repo wiring section,
   **Then** the runbook documents pinning the context bus — `git submodule add` of `ctam-architecture` at `_arch`, checked out at an exact `arch-vN` tag — and that the pin moves only by an explicit, recorded bump,
   **And** it documents generating `CLAUDE.md` from `_arch/agent-rules/enforcement/claude/CLAUDE.md.template`, substituting `{{SERVICE_NAME}}` and `{{ARCH_VERSION}}` and changing nothing else,
   **And** it documents copying `.claude/settings.json` from `settings.json.template` together with the `hooks/` directory, and making the hooks executable,
   **And** it records how to verify the wiring actually took: a session in the new repo is **denied** a commit while HEAD is on `main`.

4. **Given** the runbook is complete,
   **When** it is published to the bus,
   **Then** it is linked from `_arch/agent-rules/index.md` and from `architecture/starter-template.md`, so a session finds it without knowing the path,
   **And** it carries the `arch-vN` tag it was published under, since downstream repos pin the bus by tag.

## Tasks / Subtasks

- [x] Task 1 — Create `runbooks/` and the repo-creation section (AC: 1)
  - [x] `mkdir runbooks` — the directory does not exist yet in this repo; this story creates it
  - [x] Write the web-UI repo-creation steps: HMCTS org, **private**, CNP naming (`ctam-{service}`, `{product}-shared-infrastructure`)
  - [x] Write the explicit prohibition on `setup-new-repo.sh` and state the reason (it makes the repo PUBLIC) — cite `agent-rules/enforcement/claude/hooks/block-git-writes.sh`, which records this as a reason `gh` stays closed
  - [x] Write the branch-protection steps for `main`: require PR review, require status checks, require linear history
  - [x] Write the `CODEOWNERS` + team-access steps (AR29)
  - [x] Self-check: grep the finished runbook for `gh ` and confirm zero hits
- [x] Task 2 — Document the scaffolding boundary and the consumer list (AC: 2)
  - [x] State that `ctam-scaffold.sh` scaffolds **locally** and pushes to a **pre-created** remote over plain `git`, and never creates or configures a repository
  - [x] List the five stories this runbook is a precondition of: 0.0.1, 0.1.1, 0.2.1, 0.2.4, 0.5.1
- [x] Task 3 — Document the per-repo agent-enforcement wiring (AC: 3)
  - [x] `git submodule add` of `ctam-architecture` at `_arch`, checked out at an exact `arch-vN` tag; state the bus-pinning rule (the pin moves only by an explicit, committed bump)
  - [x] Generating `CLAUDE.md` from `agent-rules/enforcement/claude/CLAUDE.md.template`, substituting `{{SERVICE_NAME}}` and `{{ARCH_VERSION}}` only
  - [x] Copying `.claude/settings.json` from `agent-rules/enforcement/claude/settings.json.template` plus the `hooks/` directory, and `chmod +x` on the hooks
  - [x] The verification step: a session in the new repo is DENIED a commit while HEAD is on `main`
- [x] Task 4 — Wire the runbook into the bus's own navigation (AC: 4)
  - [x] Add a link from `agent-rules/index.md`
  - [x] Add a link from `architecture/starter-template.md`
  - [x] Record in the runbook the `arch-vN` tag it is published under
- [x] Task 5 — Self-review against the ACs and hand back (AC: 1,2,3,4)
  - [x] Walk each AC clause and point at the runbook heading that satisfies it; anything unsatisfied is a gap, not a judgement call
  - [x] Set `Status:` to `review` (**W13**) and write the handoff (**W12**)

## Dev Notes

**This packet targets the context-bus repo itself (`ctam-architecture`), not a service repo.** Three consequences, all of which differ from every other CTAM packet:

- **There is no `_arch/` submodule here** — the bus cannot submodule itself. Every path in this packet is **repo-root-relative**: the rules are at `agent-rules/…` and the architecture at `architecture/…`. If you find yourself looking for `_arch/`, you are in the wrong mental model.
- **There is no `CLAUDE.md` and no `.claude/hooks/` in this repo.** `CLAUDE.md.template` is written for `ctam-{service}` execution units, and nobody has authored a bus-repo equivalent. So no rules load automatically and **`block-git-writes.sh` is not installed here** — the `main`-branch discipline in **R13**/**W7** is on you as the operator, not enforced by a hook. Do not commit to `main`. You are on `story/0.0.0`; stay on it.
- **`agent-rules/index.md` binds only the twelve Java execution units** (`ctam-mock-auth` … `ctam-mi-feed`), and explicitly records `ctam-ui`, `ctam-admin-ui` and `ctam-shared-infrastructure` as *not yet covered*. `ctam-architecture` is not in either list. The **core rules R1–R14 still bind your conduct**; the Java- and test-specific rules do not apply to a Markdown deliverable. See *Definition of done* below for exactly which gate items are waived.

### Path translation — read this before the ACs

The acceptance criteria above are **verbatim from the epic** and are never edited (**W8**). They were authored assuming a *service* repo, so four clauses name `_arch/…` paths and one names `ctam-architecture/runbooks/…`. **In this repo those prefixes do not resolve** — the bus cannot submodule itself, and you are already inside `ctam-architecture`. Translate as you read:

| The AC says | In this repo it is |
|---|---|
| `_arch/agent-rules/enforcement/claude/hooks/block-git-writes.sh` | `agent-rules/enforcement/claude/hooks/block-git-writes.sh` |
| `_arch/agent-rules/enforcement/claude/CLAUDE.md.template` | `agent-rules/enforcement/claude/CLAUDE.md.template` |
| `_arch/agent-rules/index.md` | `agent-rules/index.md` |
| `ctam-architecture/runbooks/github-setup.md` | `runbooks/github-setup.md` |
| `architecture/starter-template.md` | unchanged — already repo-root-relative |

The *content* requirement is unaffected: the runbook you write is for engineers setting up **service** repos, so the runbook's own text should keep the `_arch/` form when instructing a reader to wire up a service repo. Only your navigation while implementing changes.

**This is a dispatch-side defect, recorded not fixed** (**W8**, **R14**): the epic's ACs should not embed `_arch/` for a story that targets the bus. Raised under *Open questions* for the control plane to correct in `epic-0.0-platform-estate-provisioned.md`; do not edit the epic from here (**W6** — the control plane is a different repository).

### Context distilled from the bus

Read at the pin, `arch-v1.1` (`git show arch-v1.1:<path>`). At the time of dispatch the `arch-v1.1` tree is **identical to `main`**, so the working tree is a faithful copy of the pin.

- **The `gh` prohibition is real and enforced, not advisory.** `agent-rules/enforcement/claude/hooks/block-git-writes.sh` denies `gh`/`hub` outright, with the recorded reason: *"repository administration, PR approval and merge are the human gate itself"* — and, directly relevant to AC-1, *"the HMCTS template's `setup-new-repo.sh` makes a repository PUBLIC, which is another reason `gh` stays closed."* That sentence is the authority for the prohibition the runbook must carry; quote it.
- **The bus-pinning rule** (`architecture/delivery-operating-model.md` → *The bus-pinning rule*, canonical in the control plane): *"A service repo re-syncs to a newer `ctam-architecture` version only by an explicit, committed submodule bump. The bus never mutates a downstream repo silently."* AC-3's submodule section must state this, not just the `git submodule add` mechanics.
- **Per-repo wiring layout** (`agent-rules/index.md` → *How these rules reach a service repo*) is the authoritative shape to document: `CLAUDE.md` generated from the template and naming the pinned version, `_arch/` as the submodule, `docs/stories/<id>.md` for the packet.
- **Branch naming** (`architecture/conventions.md` → *Git conventions*, amended 2026-08-20): `story/{story-id}` for dispatched story work, one branch per story from dispatch to PR; `bugfix/`, `chore/`, `feature/` for everything else. The runbook's branch-protection section should not contradict this.
- **Commit style** (same heading): Conventional Commits prefix, imperative, ≤ 72-char subject. This story's commits are `docs:`.

### Rules that apply

Core conduct rules, from `agent-rules/00-core.md` @ `arch-v1.1`. These four will actually bite on this story:

- **R4 — Cite or ask.** Every non-obvious claim in the runbook names its authority. A step that says "enable branch protection" without pointing at AR29 or the AC is a guess. This applies to the *runbook you write*, not only to your session notes.
- **R5 — Unknown means stop.** You will hit at least one genuine unknown: HMCTS org-level settings, and which team owns `CODEOWNERS` for each repo, are not recorded anywhere in the bus. Do **not** invent a team name or an org policy. Write it into *Open questions* in this packet and leave the runbook with an explicit `TBD — what is unknown, who can answer` marker, which is a documented gap rather than a fabricated instruction.
- **R6 — No unsanctioned surface.** The deliverable is `runbooks/github-setup.md` plus two link edits. Do not add a template, a script, a CI workflow or a second runbook.
- **R14 — No uninvited work.** `agent-rules/index.md` has stale frontmatter at the pin (`bus_version: unpublished (targets arch-v1.0)`) and `agent-rules/60-session-protocol.md` **W12**'s example handoff still shows `branch: feature/CTAM-123-mrd-ingestion`, which contradicts **W7**'s one-branch-per-story rule. **Both are real defects. Neither is yours to fix in this story** — record them under *Open questions*.

**R8** (nothing unfinished ships) needs one clarification for a documentation story: a `TBD` marker that AC-1 through AC-4 do not require, and that R5 forces you to record, is *not* an R8 violation — it is the honest output of a stop. A vague sentence that pretends to be an instruction is.

### Project Structure Notes

Expected file layout after this story:

```
ctam-architecture/
├── runbooks/
│   └── github-setup.md          NEW — the whole deliverable
├── agent-rules/index.md         UPDATE — one link added (AC-4)
└── architecture/
    └── starter-template.md      UPDATE — one link added (AC-4)
```

`runbooks/` does not exist yet; this story creates it. Two sibling runbooks are *planned* but not yet written — `runbooks/reference-data-maintenance.md` (Story 0.3.1) and `runbooks/identity-bootstrap.md` (Story 0.4.1) — so you are setting the house style for the directory. Keep the structure simple and copyable: numbered steps, one action per step, the authority cited beside anything non-obvious.

Read both UPDATE files completely before editing them, and add only a link — no restructuring (**R14**).

### Out of scope / boundaries

- **Creating any repository.** This story writes the procedure; it does not execute it. No repo is created, no branch protection is configured.
- **`ctam-scaffold.sh` itself** — Story 0.1.1; inventory in `architecture/starter-template.md` §B.
- **Any Azure resource** — Stories 0.0.1–0.0.5.
- **GitHub org-level policy, SSO or team provisioning** — HMCTS platform team, outside CTAM entirely.
- **A bus-repo `CLAUDE.md`.** This repo lacking one is a real gap (see *Dev Notes* above) but authoring it is not this story. Record it as an Open question.
- **Tagging the next `arch-vN`.** AC-4 asks the runbook to *record* the tag it is published under. Creating the tag is a human release action and is denied to sessions.

### Recorded deviations

- **`bus_version: arch-v1.1` with no submodule pin to read it from.** Every other packet takes `bus_version` from the target repo's `_arch` pin. This repo has no such pin, so the value records the **currently published tag** the work starts from, per the BUS-REPO STORIES rule in the control plane's `_bmad/custom/bmad-create-story.toml`. Verified at dispatch: `arch-v1.1` and `main` have identical trees.
- **`frs: []` / `nfrs: []`.** Epic 0.0 records *"FRs covered: none — this is foundational platform infrastructure with no functional-requirement surface."* The epic's NFR list (NFR10, NFR11, NFR16, NFR25–NFR28, NFR31, NFR40) is exercised by the estate stories 0.0.1–0.0.5, not by a runbook. Empty is correct here, not an omission.
- **No previous-story intelligence.** 0.0.0 is the first story in epic 0.0, so there is no preceding sibling. Story 0.6.1 (`done`) also delivered into this repo — it published the bus and tagged `arch-v1.0` — but it predates story packets, so there is **no `docs/stories/0.6.1.md` to carry forward from**. `docs/stories/` is created by this story. Nothing was skipped; there is genuinely nothing to read.
- **Dispatch landed on a pre-existing local `story/0.0.0`.** The branch had been cut by hand before dispatch, sat at exactly `main` with no commits and no upstream. Dispatch reused it rather than cutting a second branch (one branch per story, `conventions.md` → *Git conventions*).

### Definition of done

`agent-rules/90-definition-of-done.md` @ `arch-v1.1` applies to conduct. The deltas for a Markdown-only story in the bus repo — stated explicitly so no waiver is silent:

| Item | Status here | Why |
|---|---|---|
| **Q1** every AC has a passing test | **Waived** | No executable surface. Replaced by Task 5: walk each AC clause and name the runbook heading that satisfies it |
| **Q2** every behaviour driven red-first (**R2**) | **Not applicable** | **R2** governs edits to `src/main/**`. There is no `src/` in this repo and no code in this story |
| **Q3** full gate passes (`./scripts/verify.sh`) | **Not applicable** | `scripts/verify.sh` does not exist in this repo — it is a Java-service gate. Confirmed absent at dispatch |
| **Q4** no unfinished work (**R8**) | **Applies** | With the `TBD` clarification in *Rules that apply* above |
| **Q5** no unsanctioned surface (**R6**) | **Applies** | Deliverable is one new file plus two link edits. Nothing else |
| **Q6** contract tests + Spectral | **Not applicable** | No endpoint |
| **Q7** schema change proved by an `*IT` | **Not applicable** | No schema |
| **Q8** nothing forbidden logged or stored (**R11**) | **Applies** | Do not put a real person's name, email or team handle in the runbook — use role descriptions |
| **Q9** packet is current (**W8**) | **Applies** | Keep *Open questions* and `Status:` current as you go |
| **Q10** diff is minimal and scoped (**R14**) | **Applies** | Three files. Any fourth needs a citation |
| **Q11** anything not done is stated (**W10**) | **Applies** | Especially any `TBD` you leave |
| **Q12** handoff carries the signal fields (**W12**) | **Applies** | Use `branch: story/0.0.0` — W12's own example is stale, see *Rules that apply* |
| **Q13** `Status:` reads `review`, never `done`; control plane untouched (**W13**) | **Applies** | Do not edit `ctam-analysis`. A human flips `sprint-status.yaml` |

### References

- [Source: `agent-rules/00-core.md` — R1–R14 table] @ `arch-v1.1`
- [Source: `agent-rules/60-session-protocol.md` — W7 *the pull request is the human gate*, W8, W10, W12, W13] @ `arch-v1.1`
- [Source: `agent-rules/90-definition-of-done.md` — Q1–Q13 table] @ `arch-v1.1`
- [Source: `agent-rules/index.md` — *Who this binds*, *How these rules reach a service repo*] @ `arch-v1.1`
- [Source: `agent-rules/enforcement/claude/hooks/block-git-writes.sh` — the `gh`/`hub` deny block and its `setup-new-repo.sh` PUBLIC-repo rationale] @ `arch-v1.1`
- [Source: `agent-rules/enforcement/claude/CLAUDE.md.template` — the `{{SERVICE_NAME}}` / `{{ARCH_VERSION}}` substitution contract] @ `arch-v1.1`
- [Source: `agent-rules/enforcement/claude/settings.json.template` — PreToolUse hook registration] @ `arch-v1.1`
- [Source: `architecture/conventions.md` — *Git conventions*, amended 2026-08-20] @ `arch-v1.1`
- [Source: `architecture/starter-template.md` — §B starter inventory; the AC-4 link target] @ `arch-v1.1`
- [Source: control plane `_bmad-output/planning-artifacts/epics/phase-0/epic-0.0-platform-estate-provisioned.md` — Story 0.0.0; ACs copied verbatim]
- [Source: control plane `_bmad-output/planning-artifacts/architecture/delivery-operating-model.md` — *The bus-pinning rule*; *Decision 1 — context bus binding*]
- D10 (`gh` CLI unavailable — manual GitHub web-UI setup) · AR29 (`CODEOWNERS` + PR template) · AR53 revised (dedicated `ctam-shared-infrastructure`) · gaps.md G9

## Open Questions

Recorded, not resolved. Nothing below was worked around; each one is either a `TBD` in the runbook or a defect for someone else's story.

**Genuine unknowns forced into the runbook as `TBD` markers (R5):**

1. **TBD-1 — HMCTS org-level settings and repository-creation rights.** Who may create a repository in the `hmcts` org, and which org-level policies apply to a new private repo (SSO enforcement, org rulesets, default permissions, allowed merge methods) — none of it is recorded in the bus or the control plane. It matters because an org ruleset could override Part B's branch protection, and because org policy might force protection at creation time, in which case the scaffold's first push has to go via a branch and a PR rather than straight to `main`. **Who can answer:** HMCTS platform / GitHub org administrators. Marked `TBD-1` in the runbook.
2. **TBD-2 — the owning GitHub team per repository.** `CODEOWNERS` and team access need a real GitHub team handle per repo. `architecture/repo-structure.md` records only the owner's *role* ("CTAM Pathfinder team + service-specific reviewers", "platform/infra team", "admin-team scoped"); a role is not a handle, and no repo → team mapping exists anywhere. Inventing one would silently route review to the wrong people. **Who can answer:** CTAM delivery lead, with the HMCTS org administrators who provision teams. Marked `TBD-2` in the runbook.

**Conflict found while implementing AC-4 — needs a control-plane decision:**

3. **AC-4's link into `architecture/starter-template.md` edits a published mirror.** `architecture/PUBLISHED.md` states that every `.md` beside it is a mirror of `ctam-analysis/_bmad-output/planning-artifacts/` and *"editing a file here creates exactly the drift the delivery operating model exists to prevent, and the next publish would silently overwrite it."* AC-4 requires the link, and the packet's *Project Structure Notes* names the file as an UPDATE, so the edit was made — the story's ACs outrank the published architecture in `CLAUDE.md`'s precedence order. **But it will be lost the next time `publish-arch.sh` runs.** To make it durable, the same one-line callout has to be added to the canonical `planning-artifacts/architecture/starter-template.md` in the control plane and re-published. That is a control-plane edit and therefore not mine (**W6**). The `agent-rules/index.md` link has no such problem — that directory is authored on the bus, not mirrored.

**Pre-existing defects, recorded not fixed (R14):**

4. **The epic's ACs for a bus-repo story embed `_arch/` paths** that cannot resolve in `ctam-architecture` (the bus cannot submodule itself). Already recorded in *Dev Notes → Path translation*; the fix belongs in the control plane's `epic-0.0-platform-estate-provisioned.md`.
5. **`agent-rules/index.md` has stale frontmatter** — `bus_version: unpublished (targets arch-v1.0)`, while `arch-v1.1` is published and this story's `bus_version`. Not touched; this story's edit to that file is the AC-4 link only.
6. **`agent-rules/60-session-protocol.md` W12's example handoff shows `branch: feature/CTAM-123-mrd-ingestion`**, which contradicts W7's own one-branch-per-story rule and `conventions.md` → *Git conventions* (`story/{story-id}`, amended 2026-08-20). Not touched.
7. **This repo has no `CLAUDE.md` and no `.claude/hooks/`,** so no rule loads automatically here and `block-git-writes.sh` is not installed on the bus repo itself — the repo that authors the rule is the one repo not covered by it. `agent-rules/index.md` → *Who this binds* lists only the twelve Java execution units; `ctam-architecture` appears in neither list. Out of scope here (packet says so explicitly); worth a story.
8. **`arch-v1.2` is not tagged.** AC-4 asks the runbook to record the tag it is published under; the runbook's *Publication* section names `arch-v1.2` as **pending** and instructs the releaser to replace `pending` with the tag actually cut. Creating a tag is a human release action, denied to sessions.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context) — `claude-opus-5[1m]`

### Debug Log References

No executable gate exists in this repo (`scripts/verify.sh` is a Java-service gate and is absent — confirmed, per Q3). The two checks that were actually run:

- **AC-1 "no step invokes `gh`"** — word-bounded search over the finished runbook:
  `grep -nE '(^|[^[:alnum:]_`])(gh|hub)([^[:alnum:]_`]|$)' runbooks/github-setup.md` → **one hit**, line 44, inside the verbatim quotation of `block-git-writes.sh`'s deny reason (prose, not a step). Command-position search — `grep -nE '(^|[;&|(])[[:space:]]*(gh|hub)([[:space:]]|$)'` → **zero hits**. Every other mention is backticked prose naming the prohibited tool. Note the plain `grep 'gh '` in Task 1 would have been useless: it matches "through", "eight" and anything else with that bigram.
- **Link targets resolve** — every relative link in the three touched files was resolved against the filesystem; zero broken.

### Completion Notes List

- **The runbook is written in execution order, not AC order,** with an explicit sequencing callout in Part B: team access → scaffold push → `CODEOWNERS` + branch protection. Two mechanical facts force it — branch protection on `main` refuses the scaffold's first push, and `CODEOWNERS` is a file that cannot exist in an empty repo. The ACs specify content, not ordering, so all four are still satisfied; *Acceptance check* in the runbook maps each clause to its section (Task 5).
- **Two `TBD` markers were left deliberately** (Open Questions 1–2). Per the packet's R8 clarification these are the honest output of a stop, not unfinished work: each names what is unknown and who can answer it, rather than pretending to be an instruction.
- **Three non-obvious things were stated with their authority rather than left implicit,** all cited in the runbook: that the recorded submodule pin is the *commit the tag points at* (so "never point `_arch` at a branch"); that GitHub only lists a status check once it has reported, so the required-checks box has to be revisited after the first CI run; and that a non-executable hook fails **open**, which is why Step 7's `chmod` is not optional and Step 8's denial has to be observed rather than assumed.
- **`runbooks/` house style set for its two planned siblings** (`reference-data-maintenance.md` Story 0.3.1, `identity-bootstrap.md` Story 0.4.1): light frontmatter; a scope table stating what the runbook is *not*; numbered steps in execution order, one action per step; the authority named beside anything non-obvious; an *Acceptance check* table; *Open questions* with named answerers; a *Publication* section recording the `arch-vN` tag.
- **`architecture/starter-template.md` was edited despite being a published mirror** — see Open Question 3. One block-quote added above *CTAM Pathfinder scaffolding script*, nothing restructured. It needs mirroring in the control plane or the next publish erases it.
- **Not done, and not in scope:** no repository was created, no branch protection configured, no tag cut (all three are human/out-of-scope per the packet). `runbooks/terraform.md` (gaps.md G9.1) is a different runbook and was not started.

### File List

| File | Change |
|---|---|
| `runbooks/github-setup.md` | **NEW** — the deliverable |
| `agent-rules/index.md` | one link added under *How these rules reach a service repo* (AC-4) |
| `architecture/starter-template.md` | one block-quote with a link added above *CTAM Pathfinder scaffolding script* (AC-4; see Open Question 3) |

Three files, no fourth. `docs/stories/0.0.0.md` (this packet) is the working record, per W8.
